[env:raspberrypi_5]
platform = linux_arm
framework = libgpiod
board = raspberrypi_5

; Upload configuration (optional)
; Uncomment to enable automatic upload via SCP
;upload_protocol = scp
;upload_port = pi@raspberrypi.local:/home/pi/libgpiod_blink
;upload_run_after = true
